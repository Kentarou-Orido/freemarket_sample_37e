.default-countainer
  = render "default/default-header"
  %main.white
    %section.top-carousel
      .owl-stage-outer
        .owl-stage
          .owl-item
            %a.top-banner
      .owl-nav
        .owl-prev
        .owl-next
      .owl-dots

    = render "items/pickup"
    = render "items/pickup"
  = render "default/default-footer"
  = render "default/footer-sell-btn"
